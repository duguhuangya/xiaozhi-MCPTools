# 小智 MCP 连接器 - Code Wiki

## 目录

- [项目概述](#项目概述)
- [项目结构](#项目结构)
- [核心模块说明](#核心模块说明)
- [主要工具函数](#主要工具函数)
- [依赖关系](#依赖关系)
- [项目运行方式](#项目运行方式)
- [开发扩展指南](#开发扩展指南)

---

## 项目概述

### 基本信息

**项目名称**: 小智 MCP 连接器

**开发者**: 粽子同学

**项目描述**: 一个图形化界面的 Windows 下 MCP（Model Context Protocol）工具服务器，提供丰富的本地操作和开放 API 调用功能。

### 功能特点

1. **电脑控制**
   - 运行程序、打开 URL
   - 执行 CMD 命令
   - 文件操作和剪贴板管理
   - 系统控制（关机、音量、截图等）
   - 微信消息自动化发送
   - 桌面壁纸更换

2. **开放 API 工具**
   - 获取心灵毒鸡汤、抖音热点
   - 获取随机一言、舔狗日记
   - 查询星座运势、运势抽签
   - 获取百度/知乎/微博实时热榜
   - 获取名人名言、每日一句
   - 查询油价、历史上的今天
   - 高铁票查询等

3. **ALAPI 工具**
   - 更多第三方 API 集成

4. **第三方软件控制**
   - 洛雪音乐播放控制
   - PPT 翻页控制

---

## 项目结构

```
/workspace
├── MCP_Windows/
│   ├── Windows.py                  # 基础版 MCP 服务器
│   ├── Windows+OPEN_API.py         # 集成开放 API 工具版
│   ├── Windows+AL_API.py           # 集成 ALAPI 工具版
│   ├── MCP_Pipe-DEBUG.py
│   ├── MCP_Pipe-INFO.py
│   ├── Requirements.txt            # Python 依赖库列表
│   ├── 数据/                       # 数据目录
│   │   ├── 多智能接入地址/
│   │   └── 默认MCP地址.INI
│   └── 组件/                       # 服务组件目录
│       ├── MCP工具服务组件/
│       ├── 公告服务/
│       ├── 回声洞服务/
│       ├── 存储占用感知服务/
│       ├── 开机自启动/
│       ├── 快捷跳转/
│       ├── 更新服务/
│       ├── 激活服务/
│       └── 登记服务/
├── 图标/
│   ├── MCP.ico
│   └── MCP灰图标.ico
├── 注册ComDlg32.OCX组件/
├── Python安装向导必看.png
├── README.md
└── 全新安装！.EXE
```

---

## 核心模块说明

### 1. MCP 服务器初始化

文件位置: `/workspace/MCP_Windows/Windows.py`

```python
from mcp.server.fastmcp import FastMCP

# 创建 MCP 服务器实例
mcp = FastMCP("WindowsToos")
```

使用 `FastMCP` 库创建一个名为 "WindowsToos" 的 MCP 服务器。

### 2. 配置管理模块

```python
current_dir = os.path.dirname(os.path.abspath(__file__))
programs_file_path = os.path.join(current_dir, "预设", "程序预设.txt")
commands_file_path = os.path.join(current_dir, "预设", "命令预设.txt")

def load_presets(file_path: str) -> dict:
    """
    从指定的文本文档中加载预设信息。
    参数： file_path: 文本文档的路径
    """
    presets = {}
    # 加载和保存预设程序和命令

def get_default_content(file_path: str) -> str:
    """
    根据文件路径返回对应的默认内容。
    """
```

### 3. 日志模块

```python
import logging

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(name)s - %(levelname)s - %(message)s')
logger = logging.getLogger('Windows_MCP_Tools')
```

### 4. 权限控制模块

```python
允许使用微信发消息工具 = False
允许使用微信发消息工具判断文件路径 = r"C:\xiaozhi\MCP\MCP_Windows\数据\允许使用微信发消息工具.DLL"
if os.path.exists(允许使用微信发消息工具判断文件路径):
    允许使用微信发消息工具 = True
```

---

## 主要工具函数

### 系统控制类

| 函数名 | 功能 |
|--------|------|
| `计算器` | 数学计算工具 |
| `运行电脑端预设软件文件或程序` | 运行预设程序或指定路径程序 |
| `在电脑上打开URL网址` | 打开指定网页 |
| `在电脑上运行CMD命令` | 执行 CMD 命令 |
| `在电脑上创建文件与写入内容` | 创建文件并写入内容 |
| `读取复制内容` | 读取剪贴板内容 |
| `填入写入一段内容` | 自动粘贴到当前位置 |
| `回车发送` | 模拟回车发送 |
| `撤销操作` | 模拟 Ctrl+Z 撤销 |
| `锁定电脑` | 锁定当前电脑 |
| `电脑关机计划` | 执行关机/重启/取消操作 |
| `设置主人电脑系统的音量` | 设置系统音量（0-100） |
| `调用系统截图工具` | 全屏/区域截图 |
| `显示电脑桌面` | 显示桌面 |
| `查看系统资源使用情况` | CPU、内存、磁盘、网络使用情况 |
| `查看电脑配置信息` | 查看详细硬件信息 |
| `获取桌面完整路径` | 获取桌面路径 |
| `设置主人电脑系统深浅色主题` | 切换系统主题 |
| `更换桌面壁纸` | 在线壁纸更换 |
| `自动搜索并打开软件程序` | 通过开始菜单搜索并启动程序 |

### 微信自动化类（需要权限）

| 函数名 | 功能 |
|--------|------|
| `向微信指定联系人发送内容` | 自动向联系人发送文本 |
| `向微信联系人发送指定文件` | 发送文件 |
| `向微信指定联系人发送复制的内容` | 发送剪贴板内容 |

### 第三方 AI 集成

| 函数名 | 功能 |
|--------|------|
| `让豆包Ai做某事` | 打开豆包并搜索内容 |
| `让KimiAi做某事` | 打开 Kimi 并发布内容 |

### PPT 控制

| 函数名 | 功能 |
|--------|------|
| `PPT_上一页或上一步` | 上一页 |
| `PPT_下一页或下一步` | 下一页 |
| `PPT_结束放映` | 结束放映 |
| `PPT_从当页开始放映` | 从当前页放映 |
| `PPT_从头放映` | 从头放映 |
| `在文档上查找内容` | 查找功能 |

### 洛雪音乐控制（需要权限）

| 函数名 | 功能 |
|--------|------|
| `洛雪音乐_搜索并播放音乐` | 搜索并播放音乐 |
| `洛雪音乐_暂停或继续播放音乐` | 暂停/继续 |
| `洛雪音乐_上一首音乐` | 上一首 |
| `洛雪音乐_下一首音乐` | 下一首 |

### 开放 API 工具类

| 函数名 | 功能 |
|--------|------|
| `获取心灵毒鸡汤` | 获取毒鸡汤 |
| `查询抖音热榜` | 获取抖音热榜 |
| `获取随机一言` | 获取一言 |
| `获取舔狗日记` | 获取舔狗日记 |
| `查询星座运势` | 获取星座运势 |
| `运势抽签` | 今日运势签 |
| `查询三大平台热点` | 百度/知乎/微博热榜 |
| `获取名人名言` | 名人名言 |
| `获取每日一句` | 中英双语每日一句 |
| `获取绕口令` | 绕口令 |
| `查询油价` | 全国油价查询 |
| `获取新年祝福语` | 祝福语 |
| `获取今日电影票房` | 电影票房 |
| `获取脑筋急转弯` | 脑筋急转弯 |
| `每日早报` | 早报 |
| `今天吃什么` | 推荐吃什么 |
| `搜索百度百科` | 百科搜索 |
| `获取历史上的今天` | 历史事件 |
| `获取万年历` | 万年历 |
| `获取深证成指` | 深证成指 |
| `查询个股行情` | 个股行情 |
| `查询公司基本面` | 公司基本面 |
| `查询高铁票` | 高铁票查询 |
| `获取回声洞` | 回声洞内容 |
| `推送巴法消息` | 巴法云推送 |

---

## 依赖关系

### 主要依赖库

从 `Requirements.txt` 文件中可以看到以下依赖:

| 库名称 | 版本要求 | 用途 |
|--------|----------|------|
| python-dotenv | >=1.0.0 | 环境变量管理 |
| websockets | >=11.0.3 | WebSocket 通信 |
| mcp | >=1.8.1 | MCP 协议支持 |
| pydantic | >=2.11.4 | 数据验证 |
| fastmcp | - | 快速创建 MCP 服务器 |
| pyautogui | - | 自动化鼠标键盘操作 |
| pyperclip | - | 剪贴板操作 |
| paho-mqtt | - | MQTT 通信 |
| requests | - | HTTP 请求 |
| beautifulsoup4 | - | HTML 解析 |
| psutil | - | 系统资源监控 |
| pycaw | - | Windows 音频控制 |

### 系统要求

- **操作系统**: Windows 10 及以上
- **Python 版本**: >= 3.10

---

## 项目运行方式

### 1. 安装依赖

```bash
cd /workspace/MCP_Windows
pip install -r Requirements.txt
```

### 2. 启动服务器

有多个版本可供选择：

#### 基础版（Windows.py）
```bash
python Windows.py
```

#### 集成开放 API 工具版（Windows+OPEN_API.py）
```bash
python Windows+OPEN_API.py
```

#### 集成 ALAPI 工具版（Windows+AL_API.py）
```bash
python Windows+AL_API.py
```

### 3. 配置预设

首次运行会自动创建预设文件：
- `预设/程序预设.txt` - 预设程序路径
- `预设/命令预设.txt` - 预设命令

你可以编辑这些文件添加自定义预设。

### 4. 启用微信功能

需要创建以下文件来启用微信功能：
```
C:\xiaozhi\MCP\MCP_Windows\数据\允许使用微信发消息工具.DLL
```

### 5. 启用洛雪音乐功能

需要创建以下文件：
```
C:\xiaozhi\MCP\MCP_Windows\数据\使用控制洛雪音乐工具.DLL
```

### 6. 使用图形界面

可以使用项目提供的图形界面工具启动和管理：
- `全新安装！.EXE` - 完整安装程序
- `MCP_Windows/组件/` 目录下的各种服务启动脚本

---

## 开发扩展指南

### 添加新的 MCP 工具

1. 在适当的文件中（如 `Windows.py`）添加新函数
2. 使用 `@mcp.tool()` 装饰器装饰函数
3. 编写详细的 docstring 说明工具的功能和参数
4. 添加适当的错误处理和日志记录

示例:

```python
@mcp.tool()
def 你的工具名(参数1: str, 参数2: int = 0) -> dict:
    """
    工具的详细描述...
    参数:
        参数1: 参数说明
        参数2: 参数说明，默认 0
    """
    try:
        # 工具逻辑...
        logger.info("执行成功日志")
        return {"是否成功": True, "结果": "结果数据"}
    except Exception as e:
        logger.error(f"执行失败: {str(e)}")
        return {"是否成功": False, "错误": str(e)}
```

### 配置 ALAPI 密钥

1. 访问 https://www.alapi.cn/ 注册并获取 Token
2. 在相应位置配置你的密钥

### 目录结构建议

对于扩展功能，建议保持以下结构：
- 核心工具放在主 Python 文件中
- 大型功能模块可以独立成文件
- 数据文件放在 `数据/` 目录
- 服务组件放在 `组件/` 目录

---

## 常见问题

### Q: 如何添加新的程序预设？
A: 编辑 `预设/程序预设.txt` 文件，格式为：`程序名=完整路径`

### Q: 微信功能无法使用？
A: 确认已创建权限判断文件，并且已在程序预设中配置了微信路径

### Q: 如何获取 ALAPI Token？
A: 访问 https://www.alapi.cn/ 注册并申请

---

## 联系方式

- QQ 群: 1036585294
- 文档: https://kcn80f4hacgs.feishu.cn/wiki/RTaHwEOp8iu6ACkXbrNcJ4GMnVg

---

## 许可证和版权

粽子同学版权所有
